__pycache__/
*.pyc
.venv/
venv/
.env
db.sqlite3

# Firebase (FCM push) SERVICE-ACCOUNT kaliti (backend, maxfiy private key) —
# HECH QACHON commit qilinmasin. `google-services.json` (Android client
# konfiguratsiyasi) esa qasddan git'ga QO'SHILADI — u maxfiy emas (faqat
# package nomi bilan cheklangan API kalit, Google buni commit qilishni
# tavsiya qiladi), aks holda har bir dev/CI qo'lda qo'yishi kerak bo'lardi.
firebase-credentials.json
media/
staticfiles/
logs/
.DS_Store

# Sinov uchun tashlangan xom 3D fayllar (FBX/OBJ/BLEND) — repo'ga kerak emas,
# konvertatsiya qilingan GLB/USDZ backend media/ orqali saqlanadi.
*.fbx
*.FBX
*.blend
*.blend1

# Qdrant lokal vektor bazasi (embedded rejim) — CLIP embeddinglar shu yerda
# saqlanadi, backfill_embeddings orqali qayta generatsiya qilinadi.
backend/qdrant_data/
