asgiref==3.12.1
Django==6.0.7
django-cors-headers==4.9.0
django-environ==0.14.0
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
pillow==12.3.0
psycopg2-binary==2.9.12
PyJWT==2.13.0
sqlparse==0.5.5

# Google Login (ID token server-side verifikatsiyasi) — qarang apps/users/google_auth.py.
google-auth==2.56.3
requests==2.34.2

# Push (FCM) — qarang apps/notifications/push.py.
firebase-admin==7.3.0

# Vizual qidiruv (CLIP embedding + Qdrant) — qarang apps/products/embedding.py.
# torch: CPU-only o'rnatish uchun --index-url https://download.pytorch.org/whl/cpu
# ishlatilgan (GPU/CUDA kerak emas, katta hajmdagi CUDA wheel'lardan qochish uchun).
torch==2.13.0
open_clip_torch==3.3.0
qdrant-client==1.19.0
channels==4.3.2
channels_redis==4.2.1
daphne==4.2.3
